# AP mode (OFF by default — see README §Networking)
# /etc/hostapd/hostapd.conf
interface=wlan0
driver=nl80211
ssid=rccar
hw_mode=g
channel=6
wmm_enabled=0
auth_algs=1
wpa=2
wpa_passphrase=changeme123
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP
